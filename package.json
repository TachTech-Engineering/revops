{
  "name": "panther-dashboard",
  "version": "1.0.0",
  "private": true,
  "description": "Web dashboard for Panther SDK",
  "scripts": {
    "dev": "docker-compose up",
    "dev:frontend": "pnpm --filter frontend dev",
    "dev:backend": "pnpm --filter backend dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.14.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
