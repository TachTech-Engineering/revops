apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: revops-staging

resources:
  - ../../base

namePrefix: staging-

patches:
  - path: frontend-patch.yaml
  - path: backend-patch.yaml

configMapGenerator:
  - name: backend-config
    behavior: merge
    literals:
      - LOG_LEVEL=DEBUG
      - CORS_ORIGINS=https://staging.panther-dashboard.example.com
      # Staging Cloud SQL instance (created by scripts/gke-cloudsql-setup.sh).
      - CLOUD_SQL_INSTANCE=revops-486917:us-central1:revops-staging-db

images:
  - name: us-docker.pkg.dev/revops-486917/gcr.io/panther-dashboard-frontend
    newTag: staging
  - name: us-docker.pkg.dev/revops-486917/gcr.io/panther-dashboard-backend
    newTag: staging
